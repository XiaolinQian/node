'use strict';

// const ServiceResult = require('../common/ServiceResult');

/**
 * 示例
 * @param {any} vairable 参数
 */
// async function getUploadURLFromDFS(vairable) {// 使用async
//     if (!vairable) {
//         return new ServiceResult('业务上的错误')
//     }
//     let data = await xxxxDao.xxxx(xxxx) // 使用await
//     return new ServiceResult(null, data)
// }

// exports.getUploadURLFromDFS = getUploadURLFromDFS
